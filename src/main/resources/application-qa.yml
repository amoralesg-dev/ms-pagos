spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/${DB_NAME:pagos_db}?allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USER}
    password: ${DB_PASSWORD}

loader:
  path: ${LOADER_PATH:C:/apps/ms-pagos/input}

logging:
  level:
    root: INFO
    com.rassini.pagos: INFO
    org.springframework: INFO
    org.hibernate: ERROR